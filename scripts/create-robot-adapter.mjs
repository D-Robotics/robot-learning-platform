#!/usr/bin/env node
import fs from 'node:fs'; import path from 'node:path';
const [id='new-robot', family='diff-drive', displayName='New RDK Robot'] = process.argv.slice(2);
if(!/^[a-z][a-z0-9-]{1,63}$/.test(id)||!['diff-drive','omni-drive','joint','custom'].includes(family)) throw new Error('usage: node scripts/create-robot-adapter.mjs <id> [diff-drive|omni-drive|joint|custom] [displayName]');
const adapter={schemaVersion:1,id,displayName,family,hardwareProfileId:`${id}-profile`,simulation:{backend:'external'},training:{observationSize:1,actionSize:1,controlHz:10,algorithm:['ppo']},telemetry:{required:[],optional:[]},action:{kind:'custom',commandTopic:'/CHANGE_ME'}};
const profile={schemaVersion:1,id:`${id}-profile`,displayName,board:{platform:'rdk-x5',family:'rdk-custom',model:'CHANGE_ME'},ros:{topics:{imu:{name:'/CHANGE_ME',type:'sensor_msgs/msg/Imu',required:false},odom:{name:'/CHANGE_ME',type:'nav_msgs/msg/Odometry',required:false},battery:{name:'/CHANGE_ME',type:'std_msgs/msg/Float32',required:false},cmdVel:{name:'/CHANGE_ME',type:'geometry_msgs/msg/Twist',required:true}}},actuator:{kind:'diff-drive',commandTopic:'/CHANGE_ME',messageType:'geometry_msgs/msg/Twist',maxLinear:.1,maxAngular:.5,watchdogMs:500},policy:{observationAdapterId:`${id}-observation-v1`,actionAdapterId:`${id}-action-v1`,observationSize:1,actionSize:1},capabilities:['CHANGE_ME'],provenance:{kind:'template',mock:true,note:'Complete and validate before use'}};
fs.writeFileSync(path.join('adapters',`${id}.json`),JSON.stringify(adapter,null,2)+'\n'); fs.writeFileSync(path.join('profiles',`${id}.json`),JSON.stringify(profile,null,2)+'\n'); console.log(`[adapter-scaffold] created adapters/${id}.json and profiles/${id}.json`);
