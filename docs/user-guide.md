# Sim2Real Lab 使用手册与最佳实践

面向 RDK X5 的 Duck 仿真、训练与部署平台。这份手册覆盖线上环境（`https://rdkstudio.d-robotics.cc/sim2real/`）和本地演示两种用法，并给出从第一次登记模型到上板预检的完整路径。

**一句话定位**：把「一个动作任务」从浏览器仿真一路推进到 X5 板端，每一步都留下可回放的证据；所有危险动作默认阻断（FAIL-CLOSED），网页永远不会直接驱动电机。

---

## 1. 快速开始

### 1.1 线上环境（推荐）

1. 打开 `https://rdkstudio.d-robotics.cc/sim2real/`。
2. 未登录时页面会显示登录卡片，点击 **「登录 RDK 账号（SSO）」**，使用 RDK Studio 同一账号完成登录，成功后自动回到本工作台。
3. 登录后顶栏右侧出现账号标识，侧栏顶部状态从「检查中」变为当前工作区就绪状态。

> 如果登录后停在主站首页，直接回到 `/sim2real/` 即可（登录态是全站共享 cookie）。已登录但工作台仍提示需要登录时，点 **「我已登录，重新加载」**。

### 1.2 本地演示（无板卡、无账号）

```bash
npm ci
npm run demo:sim2real
```

打开启动器打印的地址（默认 `http://127.0.0.1:18102/?demo=1`）。`?demo=1` 会固定 MicroDuck、行走任务、投屏视图，并预置一台 `x5-demo` 模拟板卡。退出终端会停止子服务并清理临时数据。现场投屏讲解可配合 [docs/demo-runbook.md](demo-runbook.md) 的 90 秒脚本。

### 1.3 演示视图

右上角 **「演示视图」** 按钮切换投屏友好的大字号布局（`Esc` 退出），偏好会记住。URL 参数 `?presentation=1/0` 可强制开关。

---

## 2. 界面导览

页面结构只有一层导航（左侧编号侧栏），编号即交付顺序：

| 侧栏 | 视图 | 干什么 |
| --- | --- | --- |
侧栏按「核心 / 流程 / 数据与工具」三组组织（对齐 RDK Studio 的导航范式）：

| 分组 | 入口 | 页面 | 职责 |
| --- | --- | --- | --- |
| 核心 | ◇ 工作台总览 | 状态与下一步 | 项目概况、最近活动、Agent 下一步建议、环境与连接 |
| 核心 | ✦ Agent 对话 | 对话式任务 | 计划 → 工具事件 → 证据，浮层抽屉随叫随到 |
| 流程 | 01 仿真与录制 | 仿真与录制 | 浏览器内 MicroDuck 仿真、动作任务选择、动作库、轨迹录制 |
| 流程 | 02 训练与模型 | 训练与模型 | 模型选择、套件与契约折叠面板、本地 / RoboGo 训练、运行进度 |
| 流程 | 03 评测与效果 | 评测与效果 | 关键指标、仿真/真机对比、遥测证据回放 |
| 流程 | 04 部署与上线 | 部署到 RDK X5 | 目标设备选择、设备预检（只读）与上线闸门 |
| 数据与工具 | ▦ 记录与版本 | 记录与版本 | 全部运行 / 发布 / 制品 / 遥测的审计台账 |
| 数据与工具 | ⌂ 设备上位机 | 板端上位机 | 板卡实时状态、相机流、白名单只读命令 |

**产品线选择**在侧栏项目卡内（MicroDuck / RDK Duck）；**动作任务**在仿真页、**模型**在训练页、**目标设备**在部署页——每个选择器只出现在它被使用的页面。所有视图读取同一份选择。

**状态条**（每个视图顶部）：当前模型、目标设备、任务、算力后端、台账健康——只读状态展示，不需要操作。

**顶栏**：当前项目、账号、服务连接状态（绿=就绪 / 黄=加载 / 红=需要登录或故障），以及「更多」菜单（演示视图 / 通知 / 刷新 / RDK Studio 入口）。

---

## 3. 标准工作流（契约 → 仿真 → 训练 → 评测 → 预检）

### 步骤 0：登记模型契约（训练页折叠面板）

平台按「契约」工作：模型的观测 / 动作布局、控制频率、运行时分工先说清楚，后面所有环节都按契约校验。

1. 点击 **「载入模板」** 得到 MicroDuck 参考模板（或导入你自己的 manifest JSON 文件）。
2. **「校验清单」**：本地校验字段顺序、形状、频率是否自洽。
3. **「登记版本」**：写入台账。导入 ONNX + manifest 只登记元数据和哈希，**不会执行**你上传的 Python、XML 或 shell 内容。

契约检查清单四项：产品标识（MicroDuck 与 RDK Duck 不共用机器人维度）、观测/动作布局、运行时分工（运控 CPU ONNX 单线程、感知走 X5 BPU）、制品引用（只接受可追溯校验和）。

### 步骤 1：仿真与录制（01）

- MicroDuck 产品线：页面内嵌浏览器仿真（MuJoCo WASM），键盘控制鸭子完成动作；RDK Duck 产品线需要真实适配器，否则显示等待提示。
- 右侧「当前模型可用动作」列出 manifest 里声明的动作和按键映射。
- 仿真中左下角可录制轨迹，导出 JSONL —— 这就是后续评测页可导入的证据。
- 「独立打开仿真」在新标签页全屏运行。

### 步骤 2：训练（02）

**训练后端二选一（或先用 Mock）**：

| 后端 | 何时用 | 说明 |
| --- | --- | --- |
| 本地服务器 | 默认 | 本地 worker 跑真实训练，无需外部账号；面板显示运行中 / 排队任务数 |
| RoboGo | 可选 | 云端算力，需显式登录后读取聚合状态 |
| Mock 协议演示 | 无 CUDA / 首次验证 | 只验证请求协议和台账，**不产生可部署模型**，界面上始终标明「协议演示」 |

操作：选择模型 → **「发起本地训练」**（或「登记 RoboGo 训练」）。展开「训练参数与续训」可选档位（冒烟 64 环境 / 低显存 / 标准 1024 / 高显存 4096）和续训 checkpoint。运行卡片实时显示 `queued → running → completed`，点「查看运行详情」看指标卡和原始 JSON。

### 步骤 3：评测（03）

评测页的原则是**先看证据再决定上板**，不会用默认值冒充结果：

1. **关键指标**（契约状态 / 成功率 / 跌倒率 / 控制延迟）：由训练 worker 或 X5 Agent 回写；没有数据就保持为空。
2. **效果对比**：仿真 vs 真机的成功率横条。
3. **遥测回放与证据**：导入 X5 Board Agent 或浏览器录制的 JSONL（本地解析、不上传），可视化奖励时间线、观测 / 动作热力图；确认采样率与跌倒事件后 **「上传到当前 Run 并评测」** 绑定到正式运行。
4. 页面底部的「下一步」条会根据证据状态给出建议（导入遥测 → 查看闸门 → 生成预检）。

**证据分级很重要**：`来源未验证`（上传方声明的 source）< 真实 worker 指标 / 受信 attested replay。只有后者能解锁发布推进；合成演示证据和 Mock 指标永远标明「非真实」。

### 步骤 4：部署（04）

上线闸门四步，全部 FAIL-CLOSED：

1. **契约通过** — 观测、动作、频率一致。
2. **评测与板端预检** — 指标达标 + X5 环境检查通过。
3. **无电机 Canary** — 需要板端 Board Agent。
4. **Live** — 人工批准后开放。

操作：选择目标设备 → **「探测板型」** → **「生成预检计划」** → **「执行只读预检」**（读取架构、系统目录、磁盘；不下发模型、不启动节点、不驱动电机）。预检通过且证据达标后，闸门逐级放行；Mock 运行和合成证据会被明确阻断。

### 记录与版本（05）

台账支持 全部 / Runs / 发布 / 制品 / 遥测 四类筛选和搜索，每条记录可打开详情（指标卡 + 原始 JSON）。任何「为什么这次能 / 不能上板」的问题，答案都在这里。

### 上位机（06）

板卡实时状态（CPU / 内存 / 网络 / 电源 / 运行时长 / 磁盘 + TROS 话题）、板载相机 MJPEG 流、白名单只读命令（TROS 节点 / 话题列表、磁盘用量、服务状态）+ 串流日志、真机遥测卡片（电池电压、IMU 航向；参考机型 OriginBot）。浏览器不直连板端，全部走服务端认证代理。电机控制**默认关闭**：平台与板端各有一个开关，全部显式开启后才会出现受限驱动面板（速度 ≤0.3 m/s、单命令 ≤2 s 的运动金丝雀，通道通用、`/cmd_vel` 底盘即可复用），急停永远可用。接入真实板卡见 [docs/host-station.md](host-station.md)，受限驱动的启用流程与安全边界见 [docs/actuator-drive.md](actuator-drive.md)。

---

## 4. 最佳实践

### 工作流

- **按编号走，别跳步**：训练之前先在仿真里确认动作（步骤 1），上板之前先有真实评测证据（步骤 3）。跳步的每一步都会在闸门被拦下来。
- **一次只改一个变量**：换任务、换模型、换超参不要同时进行，否则记录页无法归因。
- **用续训而不是重跑**：调参微调用「续训 checkpoint」从最近检查点继续，省时且曲线可比。
- **冒烟档先行**：先跑 smoke 档确认协议通、台账对，再切标准 / 高显存档。

### 证据与诚实性

- **上传的遥测先本地看**：导入 JSONL 后先检查采样率（50Hz）、跌倒事件再绑定 Run；`来源未验证` 标签是特性不是缺陷——它如实告诉你这份数据还只是声明。
- **别把 Mock / 合成证据当真实结果**：它们只用于演示流程，页面会一直标注。对外的数据一律来自真实 worker 回写或 attested replay。
- **命名可检索**：模型版本、任务、Run 摘要里带上关键参数（如 `walk-ppo-1024env-ep300`），记录页搜索才有效。

### 安全边界

- **预检永远只读**：不要试图绕过——它就是设计来在你承诺真机前给你完整板端画像的。
- **Canary 先于 Live**：无电机 Canary 通过、日志干净，再人工批准 Live。
- **电机控制默认不在浏览器里**：受限驱动是双开关 + 双重钳制 + 时间盒 + 急停的运动金丝雀（见 [docs/actuator-drive.md](actuator-drive.md)），默认关闭；其余任何「想让机器人动」的场景走部署流程 + 受控 Board Agent。

### 日常操作

- **服务异常先刷新**：顶栏状态点变红时，先点「刷新」；持续异常看 `/healthz`。
- **演示前用 `?demo=1`**：固定上下文 + 投屏布局 + 种子板卡，避免现场选错环境。
- **定期回看记录页**：训练失败的 Run 的 summary 会写明原因，处理完再重试（失败任务不会自动重试）。

---

## 5. 常见问题

| 现象 | 处理 |
| --- | --- |
| 页面提示需要登录 | 点登录卡片走 SSO；已登录则点「我已登录，重新加载」 |
| 仿真区空白 / 等待挂载 | MicroDuck 静态 bundle 未挂载（线上偶发）或当前产品线无浏览器适配器；刷新，或用「独立打开仿真」验证 |
| 训练按钮不可点 | 未选择模型，或训练后端未就绪——先在训练页顶部选择模型，再看算力后端面板状态 |
| 评测指标全是 `—` | 正常：指标需真实 worker / X5 Agent 回写；先完成一次真实训练或导入遥测 |
| 「生成预检计划」不可用 | 未选择目标设备，或当前模型是 Mock / 合成证据——被闸门刻意阻断 |
| 上位机无数据 | 板端 agent 未就绪；确认 `RDK_SIM2REAL_BOARD_AGENT_URL` 指向可达 agent 并已登记设备 |
| 浏览器显示旧版界面 | 强刷一次（HTML 已配置为每次重校验缓存） |

更多部署 / 运维细节：[docs/host-station.md](host-station.md)（上位机与板端 agent）、[docs/actuator-drive.md](actuator-drive.md)（受限驱动）、[docs/gpu-runner.md](gpu-runner.md)（GPU 训练机）、[docs/demo-runbook.md](demo-runbook.md)（演示脚本）、[docs/engines/starter-ppo.md](engines/starter-ppo.md)（Starter PPO 引擎）。
